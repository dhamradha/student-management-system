# ---------------------------------------------------------------------------
# Firebase Web (client) SDK — safe to expose to the browser.
# Firebase Console > Project settings > General > Your apps > Web app config
# ---------------------------------------------------------------------------
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# Firebase App Check — reCAPTCHA v3 site key (public). Leave blank to disable
# App Check locally. Get it from Firebase Console > App Check > your web app.
NEXT_PUBLIC_APPCHECK_RECAPTCHA_SITE_KEY=
# Optional: a fixed debug token for local dev (else one is printed to the
# browser console on first load — register it under App Check > debug tokens).
NEXT_PUBLIC_APPCHECK_DEBUG_TOKEN=

# ---------------------------------------------------------------------------
# Firebase Admin (server) SDK — SECRET. Never expose with NEXT_PUBLIC_.
# Firebase Console > Project settings > Service accounts > Generate new key.
# Paste the values from the downloaded JSON. Keep \n literal in the private key.
# ---------------------------------------------------------------------------
FIREBASE_ADMIN_PROJECT_ID=
FIREBASE_ADMIN_CLIENT_EMAIL=
FIREBASE_ADMIN_PRIVATE_KEY=
